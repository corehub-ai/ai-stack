{
  "name": "ia-stack",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --build --pretty",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun test",
    "corehub": "bun run packages/cli/src/index.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "typescript": "6.0.3"
  }
}
